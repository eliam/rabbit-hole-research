# Fleet (business.rivian.com/api, TIER 1) auth operations — documents recovered from
# recon/business-auth-bundle.js. Unauthenticated. ONE attempt each, garbage input only
# (no brute force, no real credentials). RoE: testing THIS host's authn/authz is the
# program's stated primary focus.
# 2026-09-26T16:01:36+01:00

### query getUserRole { currentUserRoles }   (unauth data exposure?)

### mutation ssoExchange  <-- the interesting one (token minting)

### mutation businessLogin (ONE attempt, invented credentials)

### mutation logout (no session)

# 2026-09-26T16:01:56+01:00  — ssoExchange token-validation probes (forged/garbage tokens only).

# 2026-09-26T16:02:14+01:00 — ssoExchange forwarding-path characterisation (benign token shapes only).

### Input-shape discovery via EMPTY input (single validation probe each — not enumeration).



### Incremental input-shape discovery (validation errors only; no value guessing/bruteforce)


