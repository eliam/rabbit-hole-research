# APK secrets triage — `android/com.rivian.android.consumer/rivian-secrets.txt`
# Source APK: base.apk (44.7 MB), com.rivian.android.consumer v3.16.0 (versionCode 4897),
#             released 2026-09-03, minSdk 33.
# NO SECRET VALUES ARE RECORDED IN THIS FILE, BY DESIGN. Values are described by
# detector, length and shape only. The raw dump itself is gitignored.
#
# VERDICT: no valid secret. The dump is a generic scanner output and is ~100%
# false positive. Do not re-run a scanner on this APK expecting gold.

## Detector categories in the dump (497 entries total)
#   [LinkFinder]           397  -> URLs/paths. NOISE for secrets, VALUABLE for recon.
#   [IP_Address]            71  -> ASN.1 object identifiers (0.100.1.1, 1.2.156.1, ...).
#                                 Not IP addresses. FALSE POSITIVE.
#   [JSON_Web_Token]        21  -> shortest ~32 chars; ZERO entries are jwt-shaped
#                                 (no two-dot three-segment form >=100 chars).
#                                 FALSE POSITIVE.
#   [Authorization_Basic]    4  -> values are literal English prose:
#                                 "basic =", "basic constraint", "basic constraints.",
#                                 "basic constraints". FALSE POSITIVE.
#   [Google_API_Key]         2  -> the only real-shaped items. 39 chars, "AIzaSy..." prefix.
#   [Generic_Secret]         1  -> 597-char blob that is a Stripe SDK Java class
#                                 signature ("Secret", "publishableKey", "stripeAccountId",
#                                 "<init>", "("). FALSE POSITIVE.
#   [Mailto]                 1  -> an email address. Noise.

## The two Google API keys — NOT a submittable finding
# Both are Google API keys ("AIzaSy..." prefix, 39 chars). They are real-shaped, but the
# RoE disqualifies them twice over, verbatim:
#   * "Disclosed/misconfigured API keys (Maps, DD Monitoring, etc.)"        (line 711)
#   * "API key disclosure without proven business impact"                   (line 675)
#   * Mobile: "API key leakage used for insensitive activities/actions"     (line 750)
# No business impact was proven, and no attempt was made to use them: exercising a
# Google-hosted key would send traffic to a third party, and the RoE puts third-party
# infrastructure out of scope. Recommendation to the operator: rotate/restrict, do not
# file. NOTE: "DD" in that RoE line means Datadog — the same clause covers Datadog keys.

## assets/ds-test-ec.txt and assets/ds-test-rsa.txt — NOT secrets
# These are the Datadog Android SDK's own bundled test fixtures: a base64 DER
# EC P-256 SubjectPublicKeyInfo (starts "MFkwEwYHKoZIzj0C...") and an RSA PKCS#1
# public key (starts "MIIBIjANBgkq..."). They are PUBLIC keys, not private key
# material, and they belong to the vendor's test suite, not to Rivian.

## Offsets / false-positive note for future runs
# The scanner's LinkFinder category is where the value is. Extracted host inventory
# (unique hosts referenced by the app):
#   rivian.com (18), media.rivian.com (1 - RoE CARVE-OUT, out of scope),
#   gearshop.rivian.com (1 - third-party Shopify, out of scope),
#   api.stripe.com / hooks.stripe.com / m.stripe.com / q.stripe.com /
#   errors.stripe.com / merchant-ui-api.stripe.com  -> Stripe payments
#   assets.ctfassets.net                            -> Contentful CDN
#   firebaseinstallations / firebaseremoteconfig / firebase-settings.crashlytics -> Firebase
#   www.chargepoint.com                             -> ChargePoint (EV charging)
#   octopusenergy.com/octopus-energy-rivian         -> Octopus Energy
#   js.hcaptcha.com                                 -> hCaptcha
#   apps.mapbox.com / www.mapbox.com                -> Mapbox (MapLibre also bundled)
#   waze.com, nhtsa.gov, octopusenergy.com          -> deep-link destinations
#   googleadservices / googlesyndication / app-measurement -> GA/Ads SDK noise
